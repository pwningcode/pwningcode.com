---
interface Props {
  as?: string;
  class?: string;
}

const { as: Tag = 'div', class: className = '' } = Astro.props;
---

<Tag class:list={['group relative flex flex-col items-start', className]}>
  <slot />
</Tag>
